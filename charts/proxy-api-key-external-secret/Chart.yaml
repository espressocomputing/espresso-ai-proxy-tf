apiVersion: v2
name: proxy-api-key-external-secret
description: External Secrets resources for proxy API key sync from a cloud secret store (AWS Secrets Manager or Azure Key Vault).
type: application
version: 0.2.0
appVersion: "1.0"
